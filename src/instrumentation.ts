/**
 * Hook opcional de Next.js. Debe existir el archivo si la caché de `.next`
 * referencia `instrumentation`; no ejecutar validaciones de env aquí (evita
 * problemas en dev y en hosting).
 */
export async function register() {}
